# Install with:  uv pip install -r requirements.txt
# Direct dependencies only, pinned. Transitives resolve normally so this stays readable.

# Graph / orchestration
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
langchain==1.3.15
langchain-core==1.6.0

# Model providers - instantiated directly, not via init_chat_model
langchain-openai==1.6.0
langchain-groq==1.1.3

# MCP client for the Google Drive sink
langchain-mcp-adapters==0.3.2

# Tracing (optional at runtime - see config.py)
langsmith==0.11.1

# Data + charts
# pandas held at 2.x on purpose: seaborn 0.13.2 predates pandas 3.0 and I'd
# rather not have a fresh clone break on an untested combination.
pandas==2.3.3
matplotlib==3.11.1
seaborn==0.13.2
scipy==1.18.0

# Web UI
streamlit==1.62.0

# Support
pydantic==2.13.4
python-dotenv==1.2.3
rich==15.0.0
tabulate==0.10.0
